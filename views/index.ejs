<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Fundraiser Page</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-light">
    <div class="container py-5">
      <h1>Support Our Fundraiser!</h1>
      <p>
        Our goal is to raise $<%= goal %>. We have raised $<%= raised %> so far.
      </p>

      <div class="progress mb-3" style="height: 30px">
        <div
          class="progress-bar"
          role="progressbar"
          style="width: <%= (raised / goal) * 100 %>%"
          aria-valuenow="<%= raised %>"
          aria-valuemin="0"
          aria-valuemax="<%= goal %>"
        >
          $<%= raised %>
        </div>
      </div>

      <a
        href="https://www.paypal.com/donate/?hosted_button_id=YOUR_PAYPAL_ID"
        class="btn btn-primary"
        >Donate Now</a
      >
    </div>
  </body>
</html>
